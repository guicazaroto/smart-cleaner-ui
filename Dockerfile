FROM node:20-alpine

ARG PUBLIC_API_URL
ARG PUBLIC_DEFAULT_TOKEN

ENV NEXT_PUBLIC_API_URL=${PUBLIC_API_URL}
ENV NEXT_PUBLIC_DEFAULT_TOKEN=${PUBLIC_DEFAULT_TOKEN}

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

RUN npm run build

EXPOSE 3000

CMD ["npm", "start"]